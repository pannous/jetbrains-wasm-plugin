// WIT syntax highlighting test
package example:hello@1.0.0;

/// Documentation comment for the interface
interface greet {
    /// Greet a person by name
    greet: func(name: string) -> string;

    /// A record type
    record person {
        name: string,
        age: u32,
    }

    /// An enum type
    enum color {
        red,
        green,
        blue,
    }

    /// A function using builtin types
    process: func(count: u64, ratio: float64) -> result<bool, string>;
}

world hello-world {
    import greet;
    export run: func();
}
